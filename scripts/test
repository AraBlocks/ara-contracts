#!/bin/bash

# lint
npm run lint

# installation
npm install -g ganache-cli truffle

# setup
ganache-cli --gasLimit 2000000 2> /dev/null 1> /dev/null &
sleep 5
rm -rf build
truffle compile
truffle migrate --reset

# ava
ava test/registry.js --verbose
ava test/util.js --verbose
