#!/bin/bash

# lint
npm run lint

# installation
npm install -g ganache-cli truffle

# run local ganache node
ganache-cli --port 9500 &
sleep 5
truffle migrate --reset --network local

# truffle test

# ava tests
ava test/js/* --verbose

# cleanup
pkill -f ganache 
